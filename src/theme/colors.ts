import { Color } from "@/types/theme";

const primary: Color = {
  main: "#ffc500",
  light: "#ffd74f",
  dark: "#ffb500",
  contrastText: "#000",
};

const secondary: Color = {
  main: "#444",
  light: "#666",
  dark: "#222",
  contrastText: "#fff",
};

export const colors = {
  primary,
  secondary,
};

export default colors;
