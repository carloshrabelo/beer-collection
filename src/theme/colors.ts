import { Color } from "@/types/theme";

const primary: Color = {
  main: "#ffc500",
  light: "#ffd74f",
  dark: "#ffb500",
  contrastText: "#000",
};

export const colors = {
  primary,
};

export default colors;
